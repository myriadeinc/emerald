'use strict';

const MinerService = {

    getJob: () => {
        return new Promise((resolve, reject) => {
            reject(server.error(501, 'not implemented'));
        });
    },

    submit: () => {
        return new Promise((resolve, reject) => {
            reject(server.error(501, 'not implemented'));
        });
    },

    login: () => {
        return new Promise((resolve, reject) => {
            reject(server.error(501, 'not implemented'));
        });
    }
}

module.exports = MinerService;