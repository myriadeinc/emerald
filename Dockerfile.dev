FROM node:12.18.3-buster
  EXPOSE 8080
  EXPOSE 22345
  EXPOSE 9878
  WORKDIR /usr/src/app
  COPY . .
  RUN apt-get -qq update && apt-get -qq install -y libboost-all-dev \
  libsodium-dev \
  libsodium23 

  RUN npm install --quiet
  RUN ldconfig

  # USER node
  CMD ["run.sh"]